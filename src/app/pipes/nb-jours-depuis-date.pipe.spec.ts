import { NbJoursDepuisDatePipe } from './nb-jours-depuis-date.pipe';

describe('NbJoursDepuisDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NbJoursDepuisDatePipe();
    expect(pipe).toBeTruthy();
  });
});
