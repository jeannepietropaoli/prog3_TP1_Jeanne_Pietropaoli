import { DureeHeureMinSecPipe } from './duree-heure-min-sec.pipe';

describe('DureeHeureMinSecPipe', () => {
  it('create an instance', () => {
    const pipe = new DureeHeureMinSecPipe();
    expect(pipe).toBeTruthy();
  });
});
